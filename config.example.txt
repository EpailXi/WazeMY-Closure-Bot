# =============================================================================
#  WAZE CLOSURE BOT v7 - SETTINGS          (rename to config.txt)
#  No quotes, no spaces around =
# =============================================================================

# ---- Telegram ---------------------------------------------------------------
# Must be true or nothing is sent (false = print messages to the console).
SENT_TO_TELEGRAM=false
TELEGRAM_TOKEN=
# Channel: add the bot as admin, then @channelname or the -100... id
CHAT_ID=
# Forum/topic groups only. Leave blank otherwise.
MESSAGE_THREAD_ID=
# Optional: YOUR chat id for failsafe alerts (started, cookie expired, stalled).
ADMIN_CHAT_ID=
# Optional: "still running" message to ADMIN_CHAT_ID every N hours. 0 = off.
HEARTBEAT_HOURS=0

# ---- Who counts as the app ---------------------------------------------------
# Closures made by these Waze accounts are NEVER posted (comma separated usernames).
EDITORS=
FEED_ACCOUNTS=WazeClosures
# Also skip closures Waze marks as made in WME, and Major Traffic Event closures.
SKIP_WME_CHANNEL=true
SKIP_EVENT_CLOSURES=true
# Only closures created within this many hours are posted.
FRESH_HOURS=6
# After the bot was down, look back over the gap - never more than this.
CATCHUP_HOURS=24

# ---- Countries (failsafe) ----------------------------------------------------
# Post ONLY closures whose road is in one of these countries (Waze codes,
# comma separated, e.g. MY or MY,BX). Unknown country = not posted.
ALLOWED_COUNTRIES=MY

# ---- Bot account session -----------------------------------------------------
# The cookie of the SEPARATE bot Waze account goes in cookie.txt (see README).
WAZE_COOKIE_FILE=cookie.txt
WAZE_ENV=row
# When the cookie stops working: alert, then retry this often (no restart needed).
SESSION_RETRY_MINUTES=10

# ---- Memory -----------------------------------------------------------------
# data/memory.json: every closure ID already handled, kept across restarts.
MEMORY_DAYS=30
MEMORY_MAX=50000
SEED_ON_FIRST_RUN=false

# ---- Pace & failsafes ---------------------------------------------------------
REQUEST_PAUSE_MS=1500
LAP_PAUSE_MS=60000
REQUEST_TIMEOUT_MS=30000
REQUEST_RETRIES=2
MAX_BOX_DEG=1
WATCHDOG_MINUTES=45
TZ_OFFSET_HOURS=8

# ---- npm run check ----------------------------------------------------------
# north,south,west,east
CHECK_BOX=3.25,3.00,101.55,101.80

# Optional: a SECOND bot used only for /status /restart /stop in the admin group
COMMAND_BOT_TOKEN=

# Area to check: box.json = whole managed area, box-kl.json = Kuala Lumpur test
BOX_FILE=box.json

# ---- Brunei: its own bot + channel (then set ALLOWED_COUNTRIES=MY,BX) ----
TELEGRAM_TOKEN_BX=
CHAT_ID_BX=
